# Taiwan force ledger blocker correction checkpoint

This checkpoint closes the seven mechanical blockers recorded by the independent audit at `c667fe1` while preserving the packet as `collecting`, internally unaccepted, and nonexecutable.

- The two mutable live pages are explicitly quarantined, carry retrieval and mutability metadata, expose no invented bookmark interval, and support no opening organization or relationship.
- All manifest reconciliation fields are derived. The 36 `blocked_by_unknowns` conservation records now yield 36 open conservation exceptions.
- Every inventory pool has exactly one deployment, maintenance record, and conservation scope in both directions; all record IDs are unique.
- Claims distinguish stock estimates, cohort flows, and program plans. Units, subjects, populations, component scopes, periods, and opening-stock eligibility are machine checked.
- Naval and fighter aggregation sets explicitly bind equipment, inventory, deployment/maintenance accounting, and conservation parentage; completeness and residual uncertainty remain explicit and parent-plus-child raw summation is forbidden.
- All 32 command relationships have atomic authority claims. No relationship asserts issue, reassignment, or release powers; broad organizational sources do not become executable authority.
- Recursive hidden-state checks, typed blocked allocation gates, derived summaries, and a repository-wide executable-consumer guard form the status firewall.

The regression suite includes every corruption accepted by the prior validator: fabricated live intervals, claim unit mismatch, claim subject mismatch, duplicate deployment, detached hierarchy, hidden coordinates, maintenance unit mismatch, fabricated summary, administrative-to-OPCON release, and a negated prose phrase paired with anonymous executable capacity. Additional regressions cover retrieval metadata, mutable-source reclassification, stock/flow mismatch, claim divergence, duplicate maintenance and conservation, duplicate IDs, missing siblings, missing residual state, authority label swapping, unconditional reserve release, and expired-record summary drift.

This is a correction checkpoint, not an acceptance decision. Independent reaudit remains required.
